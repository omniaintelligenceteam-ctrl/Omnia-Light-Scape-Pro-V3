# Render From Markers (Preferred Mode)

- The user provided an overlay image with colored dots.
- Each dot corresponds to exactly one fixture.
- Fixture type is determined by `markerLegend` color mapping.
- Add lighting effects centered on each dot; do not move them.
- Each dot = exactly one fixture.
- If a dot exists, its fixture must be rendered.
- If no dot exists for a fixture type/location, do not invent one.
